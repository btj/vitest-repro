import { describe, it, expect } from 'vitest';

describe('Test Suite', () => {
    it('hello world!', () => {
        console.log('Hello World!'); // Put a breakpoint here and then Step Over; notice no output appears in the Test results pane.
    });
});
